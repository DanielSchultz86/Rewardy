import 'package:flutter/material.dart';

class OpretFamilieScreen extends StatelessWidget {
  const OpretFamilieScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Opret Familie')),
      body: const Center(child: Text('Her kommer opret-flowet!')),
    );
  }
}